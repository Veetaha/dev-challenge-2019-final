version: '3'
services: 
    web:
        build:   '.'
        volumes:
            - .:/usr/app/
            - /usr/app/node_modules
        ports:
            - "80:${PORT}"
            - "9229:9229"
        command: npm run start
        env_file: .env